# PhotoVault Caddy 反向代理配置
#
# 本文件是默认的 HTTP 模式配置，适合本地开发和测试。
# PhotoVault 支持三种网络模式，选择适合你的场景：
#
# ┌─────────────────────────────────────────────────────────────────────┐
# │ 模式 1: HTTP Only (当前默认)                                        │
# │ 适用场景: 本地开发、纯内网无加密需求                                    │
# │ 配置方式: 使用本文件 (Caddyfile)，无需修改                              │
# │ 访问方式: http://<IP>:80                                            │
# ├─────────────────────────────────────────────────────────────────────┤
# │ 模式 2: 自签名 HTTPS (局域网)                                        │
# │ 适用场景: 局域网部署，无公网域名，但需要 HTTPS                           │
# │ 配置方式: cp Caddyfile.selfsigned Caddyfile                         │
# │ 访问方式: https://<IP>:443 (客户端需信任自签名证书)                     │
# │ 证书生成: ./scripts/generate-self-signed-cert.sh <IP> (可选)         │
# ├─────────────────────────────────────────────────────────────────────┤
# │ 模式 3: Let's Encrypt 自动 HTTPS (公网域名)                          │
# │ 适用场景: 有公网域名，需要正式 TLS 证书                                 │
# │ 配置方式: cp Caddyfile.production Caddyfile，修改域名                  │
# │ 访问方式: https://your-domain.com                                   │
# │ 前提条件: DNS 已解析到本机，端口 80/443 对外开放                         │
# └─────────────────────────────────────────────────────────────────────┘
#
# 切换模式后执行: docker compose up -d 即可生效

:80 {
    reverse_proxy photovault:8000
}
