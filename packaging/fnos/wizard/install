[
  {
    "stepTitle": "PhotoVault 工作目录",
    "items": [
      {
        "type": "tips",
        "helpText": "工作目录用于存放照片原件和元数据库，是 PhotoVault 最占空间的部分，请选择容量充足的存储空间。留空则使用应用自动创建的共享文件夹 photovault。"
      },
      {
        "type": "text",
        "field": "wizard_work_dir",
        "label": "工作目录（留空使用默认共享文件夹）",
        "initValue": "",
        "rules": [
          {
            "pattern": "^$|^/([^/]+/)*[^/]+/?$",
            "message": "请填写以 / 开头的绝对路径，例如 /vol1/photos；或留空使用默认位置"
          },
          {
            "max": 200,
            "message": "路径过长，请控制在 200 个字符以内"
          }
        ]
      },
      {
        "type": "tips",
        "helpText": "目录不存在时会自动创建，但它的上层目录必须已存在。若上层目录不存在（例如存储空间未挂载或路径拼错），安装会失败并给出提示，以免把照片写进系统盘。日志与识别模型不放在这里，由应用自行管理，卸载时一并清理。"
      },
      {
        "type": "tips",
        "helpText": "工作目录只能在安装时设置。之后如需更换，请先卸载（卸载不会删除照片），手动把原目录内容整体移动到新位置，再重新安装并填写新路径。"
      }
    ]
  }
]
